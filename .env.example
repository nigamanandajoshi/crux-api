# ── Doc-Squeeze v1.0.0 Environment Variables ─────────────────
# Copy this file to .env and fill in your configuration.
#
#   cp .env.example .env
#

# ── API Keys ──────────────────────────────────────────────────
# Groq API key (free at https://console.groq.com/keys)
# Required for: focus filtering, /api/extract, /api/search
# Without this key, only /api/squeeze (raw) and /api/batch (raw) work.
GROQ_API_KEY=gsk_your_key_here

# ── Server Configuration ──────────────────────────────────────
# Server port (Render/Railway set this automatically)
PORT=8000

# Environment: development, production, staging
ENV=development

# ── CORS (Cross-Origin Resource Sharing) ─────────────────────
# Comma-separated list of allowed origins for CORS
# Use "*" for development only (allows all origins)
# For production, specify exact domains: https://app.example.com,https://admin.example.com
ALLOWED_ORIGINS=*

# ── Timeout Configuration (seconds) ──────────────────────────
# Timeout for Jina Reader API requests
JINA_TIMEOUT=30

# Timeout for Groq LLM API requests
GROQ_TIMEOUT=45

# ── Content Processing ────────────────────────────────────────
# Character threshold for triggering Groq filtering
MAX_RAW_CHARS=20000

# Maximum characters to send to Groq (prevents context window overflow)
GROQ_MAX_INPUT_CHARS=60000

# Maximum tokens for Groq response
GROQ_MAX_OUTPUT_TOKENS=8192

# ── Rate Limiting ─────────────────────────────────────────────
# Rate limit for authenticated /api/* endpoints
RATE_LIMIT_SQUEEZE=10/minute

# Rate limit for unauthenticated (free tier) requests
RATE_LIMIT_FREE=5/hour

# ── Cache ─────────────────────────────────────────────────────
# Time-to-live for cached responses (seconds)
CACHE_TTL=900

# Maximum number of cached entries
CACHE_MAX_ENTRIES=500

# ── External APIs ─────────────────────────────────────────────
# Jina Reader API prefix (usually no need to change)
JINA_PREFIX=https://r.jina.ai/

# Groq model to use for filtering
# Options: llama-3.3-70b-versatile, llama-3.1-70b-versatile, mixtral-8x7b-32768
GROQ_MODEL=llama-3.3-70b-versatile
